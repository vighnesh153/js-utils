/**
 * @author Vighnesh Raut <me@vighnesh153.com>
 */

/**
 * Check if a number is odd
 *
 * @param { number } n Number under test
 * @returns { boolean } true, if the number is odd, else, false
 */
const isOdd = (n: number): boolean => {
  return n % 2 === 1;
};

export default isOdd;
